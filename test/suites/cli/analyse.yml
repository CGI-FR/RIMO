name: first run rimo
testcases:
  - name: rimo analyse pipeline
    steps:
      - script: |-
          rimo analyse ../testdata/newstruct/ ../testdata/newstruct/
        assertions:
          - result.code ShouldEqual 0
  - name: rimo schema
    steps:
      - script: |-
          rimo schema
        assertions:
          - result.code ShouldEqual 0
